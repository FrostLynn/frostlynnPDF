services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: frostlynn-pdf
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
